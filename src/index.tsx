import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Import the necessary CSS for the app
import './index.css';

// Render the App component into the DOM element with the id "root"
ReactDOM.render(<App />, document.getElementById('root'));
